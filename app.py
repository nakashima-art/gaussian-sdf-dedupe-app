import streamlit as st

st.set_page_config(page_title="Gaussian SDF Dedupe App", layout="wide")

st.title("Gaussian SDF Dedupe App")
st.write("Upload Gaussian .log files, convert to SDF, merge, and deduplicate by RMSD.")
